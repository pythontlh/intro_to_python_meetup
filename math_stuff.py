## This is a support file, to demonstrate how we can call custom functions
## that we write, inside of another script that we are writing

def squared(x):
    x = x * x
    return x


def cubed(x):
    x = x * x * x
    return x
